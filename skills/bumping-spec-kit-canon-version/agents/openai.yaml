interface:
  display_name: "Bump Spec Kit Canon Version"
  short_description: "Update both canon manifest versions"
  default_prompt: "Use $bumping-spec-kit-canon-version to bump the next minor release version, update extension/extension.yml and preset/preset.yml, and regenerate CHANGELOG.md from Conventional Commit subjects unless skipping the changelog is explicitly requested."
