# Test Speckit Canon Extension

Use `$test-speckit-canon-extension`.

If the skill is not registered yet, run one of:

```powershell
pwsh -NoProfile -File .codex/register-skill.ps1
```

```bash
bash .codex/register-skill.sh
```

After that, use `$test-speckit-canon-extension`.

Do not duplicate the workflow in this prompt file.
